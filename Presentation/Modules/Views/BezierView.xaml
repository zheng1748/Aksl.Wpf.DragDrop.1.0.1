<UserControl x:Class="Aksl.Views.BezierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Path Stretch="{Binding Path=Stretch}" 
              Stroke="{Binding Path=Stroke}"
              StrokeThickness="{Binding Path=StrokeThickness}"
              StrokeDashCap="{Binding Path=StrokeDashCap}">
            <Path.Data>
                <PathGeometry>
                    <PathGeometry.Figures>
                        <PathFigureCollection>
                            <PathFigure StartPoint="{Binding Path=StartPoint}">
                                <PathFigure.Segments>
                                    <BezierSegment Point1="{Binding Path=Point1}"
                                                   Point2="{Binding Path=Point2}"
                                                   Point3="{Binding Path=Point3}">
                                    </BezierSegment>
                                </PathFigure.Segments>
                            </PathFigure>
                        </PathFigureCollection>
                    </PathGeometry.Figures>
                </PathGeometry>
            </Path.Data>
        </Path>
    </Grid>
</UserControl>
